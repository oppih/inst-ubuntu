sudo -S apt-get install --print-uris $SOFTWARE -y --force-yes

sudo apt-get install language-pack-en #安装英文环境，这步会产生locale

4.加入优化因素：HOME文件夹内中转英。英文字体配置，vimrc配置

sudo dpkg -i install_flash_player_10_linux.deb

语言包还是个问题。

或加入grub－－hold降级功能？
